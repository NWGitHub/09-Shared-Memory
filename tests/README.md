# Visible Checks

Run the local visible check with:

```bash
./scripts/test.sh
```

This check verifies:
- no TODO placeholder output remains
- stdout matches the exact expected 7-line output
- all three tasks pass with deterministic static strings
